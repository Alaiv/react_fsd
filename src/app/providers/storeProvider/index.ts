import { StoreProvider } from './ui/StoreProvider';
import {
    StateSchema,
    StoreWithManager,
    StateSchemaKey,
    DispatchType,
    ThunkExtra,
    ThunkConfig,
} from './config/StateSchema';

export type {
    StateSchema,
    StoreWithManager,
    StateSchemaKey,
    DispatchType,
    ThunkExtra,
    ThunkConfig,
};

export {
    StoreProvider,
};
